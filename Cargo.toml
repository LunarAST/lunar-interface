[package]
name = "lunar-interface"
version = "0.1.0"
edition = "2021"
description = "Core contract types and mapping logic for LunarAST"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
chrono = "0.4"
